server:
  port: 9091
spring:
  application:
    name: vs-resource-server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090

logging:
  level:
    root: DEBUG
